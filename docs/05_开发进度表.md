# 🚀 全流程开发清单与测试节点表 (V1.1)

## 阶段一：基础设施升级

**目标**：完成数据库结构变更、预置内容注入、设置页 UI 优化。

| 序号 | 开发任务 | 关键操作点 | ✅ 测试节点（验收标准） |
| :--- | :--- | :--- | :--- |
| **1.1** | **项目环境更新** | 拉取 V1.0 代码，创建 V1.1 开发分支。 | 分支创建成功，环境启动正常。 |
| **1.2** | **数据库结构升级** | 修改 `db/index.js`，`knowledge` 表新增 `mode` 和 `chunks` 字段。 | IndexedDB 中 `knowledge` 表包含新字段，旧数据默认为 `'full'` 模式。 |
| **1.3** | **预置内容注入** | 修改 `App.vue`，新增 `initDefaultData()` 逻辑，检测并写入预置模板和文档。 | 全新环境下，工坊有“公文生成器”模板，知识库有“快速上手指南”。 |
| **1.4** | **设置页 UI 调整** | 调整 `Settings.vue`，将导出/导入按钮移至“数据管理”卡片。 | 设置页布局正确，数据管理区域可见。 |

---

## 阶段二：新手引导与核心服务

**目标**：降低新用户门槛，确保核心 AI 能力稳定。

| 序号 | 开发任务 | 关键操作点 | ✅ 测试节点（验收标准） |
| :--- | :--- | :--- | :--- |
| **2.1** | **欢迎弹窗开发** | 编写 `components/WelcomeModal.vue`，配置首次打开逻辑。 | 首次打开弹出弹窗，点击“开始”后消失，不再显示。 |
| **2.2** | **气泡提示逻辑** | 检测 API Key 状态，在设置图标旁显示红点/气泡。 | 未配置 Key 时有提示，配置后提示消失。 |
| **2.3** | **AI 服务优化** | 复用 V1.0 逻辑，确保流式输出稳定。 | AI 回复正常流式输出。 |

---

## 阶段三：提示词工坊

**目标**：完善模板功能，确保兼容预置内容。

| 序号 | 开发任务 | 关键操作点 | ✅ 测试节点（验收标准） |
| :--- | :--- | :--- | :--- |
| **3.1** | **模板管理优化** | 确保预置模板可编辑、可删除。 | 预置模板与普通模板行为一致。 |
| **3.2** | **历史记录保存** | 复用 V1.0 逻辑，确保对话结束触发保存。 | 生成内容后，历史列表新增记录。 |

---

## 阶段四：知识库架构升级（V1.1 核心难点）

**目标**：实现“全文投喂 + 智能分块检索”双模式架构。

| 序号 | 开发任务 | 关键操作点 | ✅ 测试节点（验收标准） |
| :--- | :--- | :--- | :--- |
| **4.1** | **文档解析优化** | 复用 V1.0 解析逻辑，增加字数统计判断。 | 上传文档后能正确统计字数。 |
| **4.2** | **分块逻辑实现** | 编写文档分块函数，按 500 字切分，生成 `chunks` 数组。 | 上传长文档（>5000字），数据库 `chunks` 字段有值且长度正确。 |
| **4.3** | **BM25 算法封装** | 编写 `composables/useBM25.js`，实现词频统计与相关性评分。 | 单元测试：输入问题，能从 chunks 中检索出最相关的 3 个片段。 |
| **4.4** | **模式自动识别** | 在上传流程中判断字数，自动标记 `mode`。 | 上传长文档自动标记为“智能模式”，短文档为“全文模式”。 |
| **4.5** | **检索路由逻辑** | 在 `Knowledge.vue` 中根据 `mode` 决定调用全文投喂还是 BM25 检索。 | 提问时，智能模式仅发送相关片段，全文模式发送全文。 |
| **4.6** | **UI 适配** | 文档列表显示模式标签。 | 列表项显示“全文模式”或“智能模式”标签。 |

---

## 阶段五：历史记录与收尾

**目标**：完善数据管理，进行全面验收。

| 序号 | 开发任务 | 关键操作点 | ✅ 测试节点（验收标准） |
| :--- | :--- | :--- | :--- |
| **5.1** | **历史记录功能** | 确保知识库问答（含智能模式）能正常保存历史。 | 智能模式的对话记录在历史中完整展示。 |
| **5.2** | **全流程验收** | 执行《验收标准清单 V1.1》，重点测试引导流程和双模式检索。 | **所有 Must 项通过。** |
| **5.3** | **构建与部署** | 执行 `npm run build`，部署测试。 | 构建无报错，线上功能正常。 |

---
```